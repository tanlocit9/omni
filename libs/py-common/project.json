{
  "name": "py-common",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/py-common",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run ruff check py_common tests",
        "cwd": "libs/py-common"
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run ruff format py_common tests",
        "cwd": "libs/py-common"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run pytest",
        "cwd": "libs/py-common"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv build",
        "cwd": "libs/py-common"
      }
    },
    "add": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv add {args.name}",
        "cwd": "libs/py-common"
      }
    },
    "remove": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv remove {args.name}",
        "cwd": "libs/py-common"
      }
    },
    "lock": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv lock",
        "cwd": "libs/py-common"
      }
    },
    "sync": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv sync",
        "cwd": "libs/py-common"
      }
    }
  },
  "tags": ["type:library", "scope:shared", "lang:python"]
}